[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts =
    -v
    --strict-markers
    --cov=api
    --cov-report=html
    --cov-report=term-missing
markers =
    asyncio: mark test as async
